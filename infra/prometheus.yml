global:
  scrape_interval: 30s
  evaluation_interval: 60s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: tryvanta_api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
    scrape_interval: 15s
